DESTDIR = 
INSTALL = @INSTALL@
INSTDIR = @INSTDIR@

all: 
	ant dist
	ant cobpp
	cd cs; make

doc:
	ant doc

install: 
	mkdir $(INSTDIR)
	$(INSTALL) acmlibs.jar $(INSTDIR)/
	$(INSTALL) cobpp.jar $(INSTDIR)/	
	cd cs; make install

clean:
	ant clean
	

