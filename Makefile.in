DESTDIR = 
INSTALL = @INSTALL@
ACM_HOME = @ACM_HOME@

all: 
	ant dist
	ant cobpp
	cd cs; make

doc:
	ant doc

install: 
	mkdir $(ACM_HOME)
	$(INSTALL) acmlibs.jar $(ACM_HOME)/
	$(INSTALL) cobpp.jar $(ACM_HOME)/	
	cd cs; make install

clean:
	ant clean
	

